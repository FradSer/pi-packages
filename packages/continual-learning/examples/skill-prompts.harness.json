{
  "rules": [
    {
      "id": "artifact-default-instance",
      "skill": "using-open-artifacts",
      "instructions": "Use coda0.com as the default instance unless the user specifies another host."
    }
  ]
}
