---
"@fradser/pi-plan-mode": patch
---

Allow read-only bash pipelines and conditional chains while validating every stage. Reject malformed shell syntax and unsafe command options, and exercise the production validator in regression tests.
