---
"@fradser/pi-recap": patch
---

Start recap generation when the first user prompt arrives so the TUI immediately shows that the session is being recapped, then refresh the summary after the turn completes.
