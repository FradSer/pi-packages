---
"pi-skill-router": patch
---

Ignore symlinked repository metadata files such as `CLAUDE.md` while continuing to reject symlinked directories, and use the native Pi loading spinner during fetch, install, and update operations.
