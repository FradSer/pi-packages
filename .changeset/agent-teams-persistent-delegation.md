---
"@fradser/pi-agent-teams": minor
---

Introduce persistent Agent delegation (`agent`) and symmetric shared communication (`agent_event`), transitioning from ephemeral teammates to cross-project persistent Agents.
