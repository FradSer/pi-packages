---
"@fradser/pi-impeccable": patch
---

Show only resources other than the header root in the loader's expanded `dependencies` field, and omit that field for singleton bundles. Preserve reference disclosures, byte counts, script-execution status, and the complete model-facing bundle.
