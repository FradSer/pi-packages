---
"pi-matt-pocock": minor
---

Add a local `deslop` standalone capability that removes AI slop (fabricated evidence, evidence widening, defensive clutter, mock patching, vacuous names) from a bounded change set with frozen behavior, and extend the code-review Standards axis with a cross-language AI slop baseline that skips patterns repository tooling already enforces.
