---
"pi-continual-learning": patch
---

Allow `/harness <prompt>` to route a natural-language rule request to the agent, with an explicit protocol for creating a narrow global rule in the personal `harness.local.json` while preserving and verifying existing configuration.
