---
"@fradser/pi-monitor": minor
---

Provide model-facing start acknowledgements with `monitor_id`, deliver terminal results synchronously in print/json modes without custom messages, and add `waitForTerminal` on `MonitorManager`.
