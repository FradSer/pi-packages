---
"@fradser/pi-context": patch
---

Keep `[context] research started` as the single transcript row while the async child runs: partial progress no longer renders a duplicate `[context] researching` row repeating the query, with live progress shown only in the researcher widget above the editor.
