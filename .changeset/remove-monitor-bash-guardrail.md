---
"@fradser/pi-monitor": minor
---

Remove the default bash tool-call guardrail. Monitor usage is now guidance-only, so native bash commands are no longer intercepted or required to use an allow-sync suffix.
