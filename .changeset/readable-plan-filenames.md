---
"@fradser/pi-plan-mode": patch
---

Use bounded readable topic filenames for new plans, with numeric collision suffixes. Persist the exact plan path across session reloads, resumes, review, research, and fresh implementation without renaming existing files.
